class Product {
  final int id;
  final title, description, image;
  final int price;
  final int quantity;

  Product(this.title, this.description, this.price, this.image, this.id,
      this.quantity);
}
